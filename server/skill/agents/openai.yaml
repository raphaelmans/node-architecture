interface:
  display_name: "Server Architecture"
  short_description: "Apply or scaffold modular server architecture"
  default_prompt: "Use $server to route or scaffold this task through the relevant server architecture slices, deriving unlisted runtime/framework integrations from repository evidence and current official resources."
