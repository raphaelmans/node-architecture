interface:
  display_name: "Server Architecture"
  short_description: "Apply modular server architecture guidance"
  default_prompt: "Use $server to route this task through the relevant server architecture slices."
