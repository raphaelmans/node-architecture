interface:
  display_name: "Client Architecture"
  short_description: "Apply modular client architecture guidance"
  default_prompt: "Use $client to route this task through the relevant client architecture slices."
